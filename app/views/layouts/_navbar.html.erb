<!-- 
IMPORTANT 
- replace "nil" in "link_to" tags with actual paths 
- replace placeholder logo
-->

<nav class="navbar is-fixed-top is-dark" role="navigation" aria-label="main navigation">
<!-- left side -->
    <div class="navbar-brand">
        <!-- navbar logo -->
        <%= link_to nil, class: 'navbar-item' do %>
            <%= image_tag 'https://bulma.io/images/bulma-logo.png', width: 112, height: 28, alt: 'example tag' %>
        <% end %>
        <!-- navbar burger -->
        <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false">
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
        </a>
    </div>

<!-- right side -->
    <div class="navbar-menu">
        <div class="navbar-end">
            <div class="navbar-item">
                <div class="buttons">
                    <% if current_user %>
                        <%= link_to "Sign out", destroy_user_session_path, data: {turbo_method: :delete}, class: 'button is-primary' %>
                    <% else %>
                        <%= link_to "Sign Up", new_user_registration_path, class: "button is-primary" %>
                        <%= link_to "Log In", new_user_session_path, class: "button is-light" %>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</nav>